import { FlexitemDirective } from './flexitem.directive';

describe('FlexitemDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexitemDirective();
    expect(directive).toBeTruthy();
  });
});
